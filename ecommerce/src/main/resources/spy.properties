# P6Spy 설정 파일 - 인메모리 컬렉션 사용으로 비활성화
# modulelist=com.p6spy.engine.spy.P6SpyFactory,com.p6spy.engine.logging.P6LogFactory,com.p6spy.engine.outage.P6OutageFactory

# 커스텀 포맷터 설정 (SQL 포맷팅)
# logMessageFormat=io.hhplus.ecommerce.config.p6spy.P6spyPrettySqlFormatter

# 로그 출력 방식 설정 (Slf4J 사용)
# appender=com.p6spy.engine.spy.appender.Slf4JLogger

# 제외할 로그 카테고리 설정 (info, debug, result, resultset, batch 제외)
# excludecategories=info,debug,result,resultset,batch

# 날짜 포맷 설정
# dateformat=yyyy-MM-dd HH:mm:ss

# 지원할 JDBC 드라이버 목록 (H2 인메모리 DB용)
# driverlist=org.h2.Driver

# 드라이버 등록 해제 여부 (true로 설정 권장)
# deregisterdrivers=true

# SQL 실행 시간 임계값 (1ms 이상만 로깅)
# executionThreshold=1

# 로그 메시지 최대 길이 (0이면 제한 없음)
# logMessageMaxLength=0

# 멀티 라인 SQL 포맷 사용 여부 (true로 설정 시 가독성 향상)
# multiline=true

# 배치 로깅 여부
# logMessageMaxLength=0

# 로그 파일 이름 (설정하지 않으면 콘솔로만 출력)
# logfile=spy.log

# 로그 레벨 설정
# logLevel=info