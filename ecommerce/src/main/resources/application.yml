spring:
  application:
    name: ecommerce

  # 인메모리 컬렉션 사용 - 데이터베이스 설정 불필요
  # datasource:
  #   url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
  #   driver-class-name: org.h2.Driver
  #   username: sa
  #   password:

  # h2:
  #   console:
  #     enabled: true
  #     path: /h2-console

  # jpa:
  #   hibernate:
  #     ddl-auto: create-drop
  #   show-sql: false
  #   properties:
  #     hibernate:
  #       format_sql: false
  #       use_sql_comments: true
  #       jdbc:
  #         batch_size: 20
  #       order_inserts: true
  #       order_updates: true
  #   database-platform: org.hibernate.dialect.H2Dialect

  output:
    ansi:
      enabled: always

# 로깅 설정
logging:
  level:
    io.hhplus.ecommerce: DEBUG
    org.springframework.transaction: DEBUG
    # 인메모리 사용으로 데이터베이스 로깅 불필요
    # org.hibernate.SQL: OFF
    # org.hibernate.type.descriptor.sql.BasicBinder: OFF
    # com.p6spy: INFO

# 서버 설정
server:
  port: 8080